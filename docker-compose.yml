services:
  api:
    image: ghcr.io/kopenkinda/racky-api:latest
    build:
      context: .
      dockerfile: ./apps/server/Dockerfile
    ports:
      - 3069:3001
    env_file: .env
    restart: always
